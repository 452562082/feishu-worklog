# 复制为 .env 后填值：
#   cp .env.example .env && chmod 600 .env
#
# claude CLI 的认证不用在这里配——直接 `claude auth login` 走 claude.ai
# 浏览器登录即可。launchd 跑时通过 plist 里的 USER/LOGNAME/HOME 让
# claude 能读到 keychain 里那条会话凭据。

# === FEISHU_WEBHOOK_URL（可选）===
# launchd 跑挂时往这个 URL 推一条飞书消息，第二天就不用等翻 log 才发现。
# 不配也行——会退化成 macOS 桌面通知（弹一下，不进通知中心可能错过）。
#
# 获取方式：在飞书任意群（建议建个只有自己的群）→ 设置 → 群机器人 →
# 添加机器人 → 自定义机器人 → 创建后给的 "Webhook 地址" 就是这个 URL。
FEISHU_WEBHOOK_URL=
