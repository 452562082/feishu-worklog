# 复制为 .env 后填值：
#   cp .env.example .env && chmod 600 .env
#
# === CLAUDE_CODE_OAUTH_TOKEN ===
# 长期 OAuth token，让 launchd 跑 claude CLI 时不再依赖 macOS Keychain。
#
# 为什么需要：claude CLI 几乎天天自动升级（每个新版本一个新二进制 → 新代码签名），
# 而 macOS Keychain ACL 是按代码签名锁定的。交互终端跑 claude 会弹「始终允许」
# 框、你点一下就更新 ACL；但 launchd 非交互模式没法弹框 → keychain 拒访问 →
# claude 拿不到 token → 报 403 Failed to authenticate。
#
# 生成方式（一次性，token 长期有效；需要 Claude Pro/Max 订阅）：
#   claude setup-token
# 按提示在浏览器登录后会得到一串 sk-ant-oat01-... 的 token，粘到等号后面即可。
#
# 注意：.env 已 .gitignore，不会被提交。
CLAUDE_CODE_OAUTH_TOKEN=

# === FEISHU_WEBHOOK_URL（可选）===
# launchd 跑挂时往这个 URL 推一条飞书消息，第二天就不用等翻 log 才发现。
# 不配也行——会退化成 macOS 桌面通知（弹一下，不进通知中心可能错过）。
#
# 获取方式：在飞书任意群（建议建个只有自己的群）→ 设置 → 群机器人 →
# 添加机器人 → 自定义机器人 → 创建后给的 "Webhook 地址" 就是这个 URL。
FEISHU_WEBHOOK_URL=
